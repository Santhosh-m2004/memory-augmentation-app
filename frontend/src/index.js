import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Using modern React 18 render approach
const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <App />
);
